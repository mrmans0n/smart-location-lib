apply plugin: 'android'

android {
    compileSdkVersion 19
    buildToolsVersion '19.0.3'

    defaultConfig {
        minSdkVersion 9
        targetSdkVersion 19
        versionCode 20005
        versionName "2.0.5"
    }
    buildTypes {
        release {
            runProguard false
        }
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile project(':library')
}
